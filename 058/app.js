import {add, Person, version} from '../057/hello'

const result = add(1, 2);
const harin = new Person('하린');

console.log(reault);
console.log(harin.name);
console.log(varsion);

// hello.js에서 내보낸 이름으로 가져온다
// 여기서 여러이름들을 콤마로 구분하고 {}를 안에 나열한다
// hello.js에서는 personA를 export하였지만 app,js에서 personA를 가져오지는 않았다

// 가져온 add 함수와 Person 클래스를 사용한다