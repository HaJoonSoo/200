// 배열 맨 앞 요소 추출하기 (shift)

const arr = [1, 2, 3];
console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());

// 변수 arr에 배열 [1, 2, 3]리터럴을 할당한다

// 변수 arr에 shift()를 연달아 호출한다. 마지막 호출에서 반환할 요소가 없으므로 undefined가 출력된다
