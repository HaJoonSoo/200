// 배열 마지막 요소 추출하기 (pop)

const arr = [1, 2, 3];
console.log(arr.pop());
console.log(arr.pop());
console.log(arr.pop());
console.log(arr.pop());

// 변수 arr에 배열 [1, 2, 3]을 할당한다

// 변수 arr에 pop()을 연달아 호출한다
// arr의 요소는 1, 2, 3 세 개의 요소가 들어있는데 반에 pop()는 4번 호출되었으므로
// 요소의 개수보다 많이 호출되었다 
// 해서 ubdefined 반환